const SEC = 60;
let hour, period, month;
hour = SEC * 60;
period = hour * 24;
month = period * 30;
console.log("There are " + hour + " seconds in 1 hour");
console.log("There are " + period + " seconds in 1 period");
console.log("There are " + month + " seconds in 1 month");